{
  "name": "bilal-staking-dapp-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for tracking staking contract events and user data",
  "author": "Bilal",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy --studio bilal-staking-dapp-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bilal-staking-dapp-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": { 
    "matchstick-as": "0.6.0" 
  },
  "keywords": [
    "subgraph",
    "staking",
    "defi",
    "ethereum",
    "thegraph"
  ]
}
